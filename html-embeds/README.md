# HTML Embeds

Store complete custom HTML embed source copied from Wix HTML components. Each embed should be grouped by tool or page.

Embeds that exchange data with Velo should document their `postMessage()` event names and expected payloads.

