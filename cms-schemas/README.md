# CMS Schemas

Document Wix CMS collections, field keys, permissions, hooks, indexes, and the pages or modules that depend on each collection.

These files are documentation only unless a future migration adds generated schema exports.

