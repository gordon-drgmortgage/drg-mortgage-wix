# CMS Collections

This document is the index for Wix CMS collection documentation.

Collection-specific notes live in `cms-schemas/`.

Track the following for each collection:

- Collection name and ID
- Field keys and types
- Permissions
- Connected pages
- Backend or public modules that query it
- HTML embeds or Velo files that depend on it
- Import/export notes

